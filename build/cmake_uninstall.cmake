# This file is generated by CMake for uninstalling the program
if(NOT EXISTS "/home/fyou/Documents/ported/ur-stack/ur-mavdiscovery/build/install_manifest.txt")
    message(FATAL_ERROR "Cannot find install manifest: /home/fyou/Documents/ported/ur-stack/ur-mavdiscovery/build/install_manifest.txt")
endif()

file(READ "/home/fyou/Documents/ported/ur-stack/ur-mavdiscovery/build/install_manifest.txt" files)
string(REGEX REPLACE "\n" ";" files "${files}")

foreach(file ${files})
    message(STATUS "Uninstalling $ENV{DESTDIR}${file}")
    if(EXISTS "$ENV{DESTDIR}${file}")
        exec_program(
            "/usr/bin/cmake" ARGS "-E remove \"$ENV{DESTDIR}${file}\""
            OUTPUT_VARIABLE rm_out
            RETURN_VALUE rm_retval
        )
        if(NOT "${rm_retval}" STREQUAL 0)
            message(FATAL_ERROR "Problem when removing $ENV{DESTDIR}${file}")
        endif()
    else()
        message(STATUS "File $ENV{DESTDIR}${file} does not exist.")
    endif()
endforeach()
